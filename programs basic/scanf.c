#include <stdio.h>

int main()
{
    int a,b,sum;
    printf("Enter two numbers \nS");
    scanf("%d %d", &a,&b);
    sum=a+b;
    printf("The sum of two numbers is %d+%d=%d \n",a,b,sum);

    return 0;
}